/*
    Operadores logicos
    AND  &&
    OR ||
    NO !
*/
let a = true;
let b = false;
console.log(a);
console.log(b);

//AND
console.log(`${a} && ${b} -> ${a && b}`);
//OR
console.log(`${a} || ${b} -> ${a || b}`);
//NO
console.log(`!${a} -> ${!a}`);
console.log(`!${b} -> ${!b}`);